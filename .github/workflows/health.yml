name: URL Health Check

on:
  schedule:
    - cron: "0 15 * * *"

jobs:
  check_home_url:
    runs-on: ubuntu-latest
    steps:
       - name: Check the deployed service URL
         uses: jtalk/url-health-check-action@v4
         with:
            url: https://arab4des.com
